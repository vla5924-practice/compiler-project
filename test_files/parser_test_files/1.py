def Z_0 (foo: int, Z: int, x: str) -> float:

    a: int = 1 + 2
    b: float = 3.0
    c: int = a + b
    z: float = a + b
    x: float = c + z
    y: float = (3.0 + 2) + a + b * c - 2 + 4 * a + c / z

